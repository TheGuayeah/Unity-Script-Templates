using UnityEngine;

    #ROOTNAMESPACEBEGIN#
public class #SCRIPTNAME#<T> : MonoBehaviour where T : Component
{
   public static T Instance { get; private set; }
   protected void Awake()
   {
      if (Instance == null)
      {
         Instance = this as T;
         //DontDestroyOnLoad(this);
      }
      else
      {
         Destroy(gameObject);
      }
   }
}
#ROOTNAMESPACEEND#
